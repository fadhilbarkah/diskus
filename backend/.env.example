# Diskus Backend Environment Variables

# The port where the backend API will run
PORT=3000

# Secret key used for signing JWT tokens (Change this in production!)
JWT_SECRET=super-secret-key-for-diskus-dev
